{
    "name": "keyboardlayout",
    "description": "A template keyboard layout for iOS for ease of usage",
    "version": "2.0.6",
    "main": "index.js",
    "dependencies": {
        "@smartface/contx": "^3.0.1"
    },
    "devDependencies": {},
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "postinstall": "[[ `${AC_APPCIRCLE}` != `true` ]] && sfMarketplaceService import library --packageFolder $(pwd) --wsPath $SMF_CIDE_WS_PATH"
    },
    "author": "",
    "license": "ISC"
}
